# `infra/terraform/gcp` — GCP deployment module (V2 stub)

See `../README.md` for the per-cloud matrix. This module targets Cloud Run
(or GKE as an opt-in) + Cloud SQL Postgres + GCS + Memorystore + Cloud Load
Balancer + Secret Manager + Cloud Logging.
