# `infra/terraform/azure` — Azure deployment module (V2 stub)

See `../README.md` for the per-cloud matrix. This module targets Azure
Container Apps (or AKS as an opt-in) + Azure Database for PostgreSQL + Blob
Storage + Azure Cache for Redis + Application Gateway + Key Vault +
Application Insights.
